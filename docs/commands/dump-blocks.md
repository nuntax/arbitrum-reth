# `arb-reth dump-blocks`

Print stored block headers, transaction hashes, and receipt status from a node datadir.

```sh
arb-reth dump-blocks /data/arb1 22207817 22207818
```

The command is read-only. It is useful for comparing a small set of local blocks with another implementation or an RPC response.
